# 04-配置文件模板

> 本文档提供星环空间 (Stellar Ring Space) 的配置文件模板，包括后端 application.yml、前端环境变量和 Nginx 配置。

---

## 一、后端配置文件

### 1.1 application.yml (主配置)

**srs-ctrl/src/main/resources/application.yml:**

```yaml
# ===================================================================
# SRS 智能管控平台 - 应用配置
# ===================================================================
server:
  port: 8082
  servlet:
    context-path: /ctrl
  tomcat:
    uri-encoding: UTF-8
    max-threads: 800
    accept-count: 100

# ===================================================================
# Spring Boot 配置
# ===================================================================
spring:
  application:
    name: srs-ctrl

  profiles:
    active: @spring.profiles.active@

  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

  # ===================================================================
  # 数据源配置
  # ===================================================================
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:srs_db}?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root_password}
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: ${DRUID_PASSWORD:admin123}
      filter:
        stat:
          enabled: true
          log-slow-sql: true
          slow-sql-millis: 3000
        wall:
          enabled: true
          config:
            multi-statement-allow: true

  # ===================================================================
  # Redis 配置
  # ===================================================================
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: ${REDIS_DB:0}
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 200
        max-idle: 50
        min-idle: 10
        max-wait: -1ms
      shutdown-timeout: 100ms

  # ===================================================================
  # RocketMQ 配置
  # ===================================================================
  rocketmq:
    namesrv-addr: ${ROCKETMQ_NAMESRV_ADDR:localhost:9876}
    producer:
      group: srs-producer-group
      send-message-timeout: 3000
      retry-times-when-send-failed: 2
      retry-times-when-send-async-failed: 2

  # ===================================================================
  # Elasticsearch 配置
  # ===================================================================
  elasticsearch:
    uris: ${ES_URIS:http://localhost:9200}
    connection-timeout: 5000
    socket-timeout: 30000
    request-timeout: 30000

  # ===================================================================
  # MinIO 对象存储配置
  # ===================================================================
  minio:
    endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
    access-key: ${MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${MINIO_SECRET_KEY:minioadmin}
    bucket-name: ${MINIO_BUCKET:srs-ft}
    secure: false

  # ===================================================================
  # 文件上传配置
  # ===================================================================
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB

# ===================================================================
# MyBatis-Plus 配置
# ===================================================================
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*Mapper.xml
  type-aliases-package: com.ft.**.domain
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: delFlag
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# ===================================================================
# 日志配置
# ===================================================================
logging:
  level:
    root: INFO
    com.ft: DEBUG
    com.baomidou.mybatisplus: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n'
  file:
    name: logs/srs-ctrl.log
    max-size: 100MB
    max-history: 30

# ===================================================================
# JWT 配置
# ===================================================================
jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-change-in-production}
  expiration: 86400000
  token-name: X-Access-Token

# ===================================================================
# 系统配置
# ===================================================================
sys:
  # 文件上传路径
  upload-path: /data/ft/upload
  # 允许的文件类型
  allowed-file-types: doc,docx,xls,xlsx,ppt,pptx,pdf,txt,zip,rar,png,jpg,jpeg,gif
  # 最大文件大小 (MB)
  max-file-size: 100
```

### 1.2 application-dev.yml (开发环境)

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/srs_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root_password

  redis:
    host: localhost
    port: 6379
    password:
    database: 0

  rocketmq:
    namesrv-addr: localhost:9876

  elasticsearch:
    uris: http://localhost:9200

  minio:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket-name: srs-ft

logging:
  level:
    root: INFO
    com.ft: DEBUG
```

### 1.3 application-prod.yml (生产环境)

```yaml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    password: ${REDIS_PASSWORD}
    database: ${REDIS_DB}

  rocketmq:
    namesrv-addr: ${ROCKETMQ_NAMESRV_ADDR}

  elasticsearch:
    uris: ${ES_URIS}

  minio:
    endpoint: ${MINIO_ENDPOINT}
    access-key: ${MINIO_ACCESS_KEY}
    secret-key: ${MINIO_SECRET_KEY}
    bucket-name: ${MINIO_BUCKET}

logging:
  level:
    root: WARN
    com.ft: INFO
  file:
    name: /var/log/ft/srs-ctrl.log
```

---

## 二、前端配置文件

### 2.1 环境变量文件

**srs-ctrl-ui/.env.development:**

```bash
# 开发环境配置
NODE_ENV=development

# API 基础路径
VUE_APP_API_BASE_URL=/ctrl
VUE_APP_API_CENTER_URL=/center
VUE_APP_API_GATEWAY_URL=/gateway

# 项目标题
VUE_APP_TITLE=星环空间统一身份认证平台
VUE_APP_SHORT_TITLE=SRS

# API 代理目标地址
VUE_APP_API_TARGET=http://localhost:8082

# 是否启用 Mock
VUE_APP_USE_MOCK=false

# 是否启用 WebSocket
VUE_APP_USE_WEBSOCKET=true
VUE_APP_WS_URL=ws://localhost:8082/ws

# 地图 API key
VUE_APP_MAP_KEY=your_map_key_here
```

**srs-ctrl-ui/.env.test:**

```bash
# 测试环境配置
NODE_ENV=test

VUE_APP_API_BASE_URL=/ctrl
VUE_APP_TITLE=星环空间统一身份认证平台-测试环境
VUE_APP_USE_MOCK=false
VUE_APP_USE_WEBSOCKET=false
```

**srs-ctrl-ui/.env.production:**

```bash
# 生产环境配置
NODE_ENV=production

VUE_APP_API_BASE_URL=/ctrl
VUE_APP_TITLE=星环空间统一身份认证平台
VUE_APP_USE_MOCK=false
VUE_APP_USE_WEBSOCKET=true
VUE_APP_WS_URL=wss://api.example.com/ws
```

### 2.2 vue.config.js

```javascript
const path = require('path');

module.exports = {
  // 生产环境是否生成 source map
  productionSourceMap: false,

  // 开发服务器配置
  devServer: {
    port: 3000,
    open: true,
    hot: true,
    overlay: {
      warnings: false,
      errors: true
    },

    // API 代理配置
    proxy: {
      // 智能管控平台 API
      '/ctrl': {
        target: process.env.VUE_APP_API_TARGET || 'http://localhost:8082',
        changeOrigin: true,
        ws: true,
        pathRewrite: {
          '^/ctrl': '/ctrl'
        }
      },

      // 统一认证中心 API
      '/center': {
        target: 'http://localhost:8081',
        changeOrigin: true,
        pathRewrite: {
          '^/center': '/center'
        }
      },

      // 文件上传
      '/upload': {
        target: 'http://localhost:8082',
        changeOrigin: true
      },

      // WebSocket
      '/ws': {
        target: 'ws://localhost:8082',
        ws: true
      }
    }
  },

  // 链式 webpack 配置
  chainWebpack: config => {
    // 配置 alias
    config.resolve.alias
      .set('@', path.resolve(__dirname, 'src'))
      .set('@views', path.resolve(__dirname, 'src/views'))
      .set('@components', path.resolve(__dirname, 'src/components'))
      .set('@utils', path.resolve(__dirname, 'src/utils'))
      .set('@api', path.resolve(__dirname, 'src/api'))
      .set('@assets', path.resolve(__dirname, 'src/assets'));

    // 生产环境优化
    if (process.env.NODE_ENV === 'production') {
      // 移除 console
      config.optimization.minimizer('terser').tap(args => {
        args[0].terserOptions.compress.drop_console = true;
        args[0].terserOptions.compress.drop_debugger = true;
        return args;
      });

      // Gzip 压缩
      config.plugin('compression').use(require('compression-webpack-plugin'), [{
        algorithm: 'gzip',
        test: /\.(js|css|html|svg)$/,
        threshold: 10240,
        minRatio: 0.8
      }]);

      // 分割 vendor
      config.optimization.splitChunks({
        chunks: 'all',
        cacheGroups: {
          vendor: {
            name: 'vendor',
            test: /[\\/]node_modules[\\/]/,
            priority: 10
          },
          common: {
            name: 'common',
            minChunks: 2,
            priority: 5
          }
        }
      });
    }
  },

  // CSS 配置
  css: {
    loaderOptions: {
      less: {
        lessOptions: {
          javascriptEnabled: true,
          modifyVars: {
            // Ant Design Vue 主题变量
            'primary-color': '#1890ff',
            'link-color': '#1890ff',
            'success-color': '#52c41a',
            'warning-color': '#faad14',
            'error-color': '#f5222d'
          }
        }
      }
    }
  },

  // 编译时输出警告
  lintOnSave: process.env.NODE_ENV !== 'production',

  // 是否启用 bazel 构建
  bazel: false
};
```

### 2.3 babel.config.js

```javascript
module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset'
  ],
  plugins: [
    [
      'import',
      {
        libraryName: 'ant-design-vue',
        libraryDirectory: 'es',
        style: true
      }
    ],
    [
      'import',
      {
        libraryName: 'lodash',
        libraryDirectory: '',
        camel2DashComponentName: false
      },
      'lodash'
    ]
  ]
};
```

---

## 三、Nginx 配置文件

### 3.1 nginx.conf (主配置)

```nginx
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

# 限制并发连接数
events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

# 开启高效传输模式
http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for" '
                    '"$upstream_addr" "$upstream_response_time" $request_time';

    access_log /var/log/nginx/access.log main;

    # 开启高效文件传输
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;

    # 连接超时时间
    keepalive_timeout 65;
    client_header_timeout 60;
    client_body_timeout 60;

    # 开启 gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;
    gzip_disable "msie6";

    # 限制请求体大小
    client_max_body_size 100m;

    # 隐藏 Nginx 版本号
    server_tokens off;

    # 允许跨域访问
    add_header Access-Control-Allow-Origin * always;
    add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
    add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';

    # 安全响应头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # 引入项目配置
    include /etc/nginx/conf.d/*.conf;
}
```

### 3.2 ft.conf (FT 项目配置)

```nginx
upstream ft_center {
    server 192.168.1.101:8081 max_fails=3 fail_timeout=30s;
    server 192.168.1.102:8081 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

upstream ft_ctrl {
    server 192.168.1.101:8082 max_fails=3 fail_timeout=30s;
    server 192.168.1.102:8082 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

upstream ft_gateway {
    server 192.168.1.101:8080 max_fails=3 fail_timeout=30s;
    server 192.168.1.102:8080 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

server {
    listen 80;
    server_name srs.example.com;

    # 重定向到 HTTPS (生产环境)
    # return 301 https://$server_name$request_uri;

    # 前端静态资源
    root /usr/share/nginx/html;
    index index.html;

    # 前端路由 (SPA)
    location / {
        try_files $uri $uri/ /index.html;

        # 静态资源缓存
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, no-transform";
        }

        # HTML 不缓存
        location ~* \.html$ {
            expires -1;
            add_header Cache-Control "no-store, no-cache, must-revalidate";
        }
    }

    # 统一认证中心 API
    location /center/ {
        proxy_pass http://ft_center/center/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 智能管控平台 API
    location /ctrl/ {
        proxy_pass http://ft_ctrl/ctrl/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 网关 API
    location /gateway/ {
        proxy_pass http://ft_gateway/gateway/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket 支持
    location /ws/ {
        proxy_pass http://ft_ctrl/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # 文件上传
    location /upload/ {
        proxy_pass http://ft_ctrl/upload/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        client_max_body_size 100m;
    }

    # 健康检查
    location /health {
        access_log off;
        return 200 "ok";
    }

    # 错误页面
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
```

### 3.3 SSL 配置 (HTTPS)

```nginx
# HTTPS server
server {
    listen 443 ssl http2;
    server_name srs.example.com;

    # SSL 证书
    ssl_certificate /etc/nginx/ssl srs.example.com.crt;
    ssl_certificate_key /etc/nginx/ssl srs.example.com.key;

    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # HSTS
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

    # OCSP Stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    verify_client off;

    # 其他配置同上...
}
```

---

## 四、Druid 监控配置

### 4.1 web.xml 配置

```xml
<filter>
    <filter-name>DruidWebStatFilter</filter-name>
    <filter-class>com.alibaba.druid.support.http.WebStatFilter</filter-class>
    <init-param>
        <param-name>exclusions</param-name>
        <param-value>*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*</param-value>
    </init-param>
    <init-param>
        <param-name>profileEnable</param-name>
        <param-value>true</param-value>
    </init-param>
</filter>

<servlet>
    <servlet-name>DruidStatView</servlet-name>
    <servlet-class>com.alibaba.druid.support.http.StatViewServlet</servlet-class>
</servlet>

<servlet-mapping>
    <servlet-name>DruidStatView</servlet-name>
    <url-pattern>/druid/*</url-pattern>
</servlet-mapping>
```

---

*文档版本: v1.0*
*生成时间: 2026-04-14*
